import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFF5F5F5);
  static const Color primaryAccent = Color(0xFF1E3A5F);
  static const Color secondaryAccent = Color(0xFF0ABAB5);
  static const Color text = Color(0xFF333333);
  static const Color inputBackground = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFDCE4F2);
}



// Primary Color Palette:
// Background Color:

// Light Gray (#f5f5f5): This gives a clean and modern look to your page.
// Primary Accent Color:

// Dark Blue (#1e3a5f): Use this for the brand title and button backgrounds to create a strong contrast.
// Secondary Accent Color:

// Cyan (#0abab5): Use this for hover effects and other accents to add a touch of vibrancy.
// Text Color:

// Dark Gray (#333333): Use this for text to ensure readability against the light gray background.
// Input Fields Background:

// White (#ffffff): Keep input fields clean and simple with a white background.
// Border Color:

// Light Blue (#dce4f2): Use this for input field borders to subtly tie in the blue tones.